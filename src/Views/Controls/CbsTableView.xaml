<UserControl
    x:Class="CbsContractsDesktopClient.Views.Controls.CbsTableView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="Root"
    mc:Ignorable="d">

    <Grid RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border
            Background="{StaticResource ShellPanelBackgroundBrush}"
            BorderBrush="{StaticResource ShellPanelBorderBrush}"
            BorderThickness="1,1,1,0">
            <Grid x:Name="HeaderGrid" />
        </Border>

        <ScrollViewer
            x:Name="RowsScrollViewer"
            Grid.Row="1"
            HorizontalScrollBarVisibility="Auto"
            HorizontalScrollMode="Enabled"
            VerticalScrollBarVisibility="Auto"
            VerticalScrollMode="Enabled">
            <StackPanel Orientation="Vertical">
                <Border x:Name="TopSpacer" Height="0" />
                <StackPanel x:Name="RowsHost" Orientation="Vertical" />
                <Border x:Name="BottomSpacer" Height="0" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
